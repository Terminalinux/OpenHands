# Настройка Codespace с автоматическим сохранением

## ✅ Что уже настроено

Все настроено автоматически! При следующем открытии codespace:

1. **Автоматический коммит** - каждые 5 минут
2. **Автоматический push** - сразу в репозиторий
3. **Сохранение перед отключением** - pre-push hook
4. **Восстановление при повторном входе** - git загружает последние изменения

## 📝 Команды для использования

### Быстрое сохранение
```bash
save
```
Сохранит все изменения с временной меткой.

### Проверка статуса
```bash
git-status-quick    # Краткий статус измененных файлов
gss                 # Алиас для быстрого статуса
git status          # Полный статус git
```

### Запуск приложения
```bash
make run            # Запустить frontend + backend
make start-backend  # Только backend
make start-frontend # Только frontend
docker-compose up   # Запустить в Docker
```

## 🔄 Как это работает

### При работе
```
Вы делаете изменения
        ↓
Каждые 5 минут автоматический коммит
        ↓
Автоматический push в репозиторий
        ↓
Все сохранено!
```

### При отключении
```
Вы закрываете codespace
        ↓
Pre-push hook запускается
        ↓
Все несохраненные изменения коммитятся
        ↓
Push в репозиторий
        ↓
Безопасно закрывается
```

### При повторном открытии
```
Новый codespace создается
        ↓
Git загружает последние изменения
        ↓
Все ваши работы восстановлены
        ↓
Готово к работе!
```

## 📂 Файлы конфигурации

- `.devcontainer/auto-save-daemon.sh` - Daemon для автосохранения
- `.devcontainer/post-create-setup.sh` - Настройка git hooks при создании
- `.devcontainer/save-env-config.sh` - Функции для сохранения
- `.devcontainer/setup-bashrc.sh` - Добавление команд в bashrc/zshrc
- `.githooks/auto-save.sh` - Основной скрипт автосохранения
- `AUTO_SAVE_SETUP.md` - Подробная документация

## 🔐 Безопасность

- ✅ Не сохраняются файлы в `.gitignore` (secrets, node_modules и т.д.)
- ✅ Все коммиты идут в основную ветку (main)
- ✅ Используется SSH/HTTPS автентификация GitHub
- ✅ Логи находятся в `~/.openhands-auto-save.log`

## 🚫 Отключение автосохранения

Если нужно временно отключить:
```bash
export GIT_SKIP_AUTOSAVE=1
```

Или удалить hooks:
```bash
rm -f .git/hooks/pre-push
```

## 📊 Просмотр истории коммитов

```bash
git log --oneline -10     # Последние 10 коммитов
git log --oneline -20     # Последние 20 коммитов
git show                  # Детали последнего коммита
```

## ⚠️ Важно помнить

- Автосохранение работает только для файлов, отслеживаемых git
- Крупные файлы (> 100MB) не рекомендуется коммитить
- При конфликтах merging может потребоваться ручное решение
- Логи автосохранения помогут в отладке

## 🆘 Проблемы

### "Push failed" в логах
```bash
# Проверьте интернет и доступ к GitHub
git remote -v
git push origin main
```

### Изменения не сохранились
```bash
# Проверьте статус
git status
# Мануально сохраните
save
```

### Нужно отменить последний коммит
```bash
git revert HEAD~0
# или
git reset --soft HEAD~1
```

## 📞 Контакты и поддержка

- 📖 Полная документация: `AUTO_SAVE_SETUP.md`
- 🐙 GitHub репозиторий: https://github.com/Terminalinux/OpenHands
- 📝 Git лог: `git log --oneline -20`
